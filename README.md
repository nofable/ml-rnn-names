# ML-RNN-NAMES
Working through the pytorch tutorial, but also getting it working on gpu to run on vast.ai.

## Install
```bash
uv sync
uv 
```

## Run tests
```bash
uv run pytest
# coverage
uv run pytest --cov=ml_rnn_names
```